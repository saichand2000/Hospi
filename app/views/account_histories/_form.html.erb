<%= form_with(model: account_history, local: true) do |form| %>
  <% if account_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_history.errors.count, "error") %> prohibited this account_history from being saved:</h2>

      <ul>
      <% account_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :patient_status %>
    <%= form.text_field :patient_status %>
  </div>

  <div class="field">
    <%= form.label :account_number %>
    <%= collection_select(:account_history, :account_id,Account.all, :id, :account_number, prompt: true) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
